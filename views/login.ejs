<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>ログイン</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <div class="wrapper">
      <main>
        <h1>ログイン</h1>
        <form method="post" action="/login">
          <div class="inputbox">
            <label for="id">ユーザ名</label>
            <input type="text" id="id" name="id" value="<%= id || '' %>" />
            <% if (status === 1) { %>
              <p class="caution">ユーザ名が適切でありません</p>
            <% } %>
          </div>
          <div class="inputbox">
            <label for="pwd">パスワード</label>
            <input type="password" id="pwd" name="pwd" value="<%= pwd || '' %>" />
            <% if (status === 2) { %>
              <p class="caution">パスワードが違います</p>
            <% } %>
          </div>
          <input type="submit" class="simple-button" value="ログイン" />
        </form>
        <div class="simple-button">
          <a href="/registration">会員登録</a>
        </div>
      </main>
    </div>
  </body>
</html>
